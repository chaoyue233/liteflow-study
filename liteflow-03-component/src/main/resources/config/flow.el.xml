<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(normalComponent);
    </chain>
    <chain name="chain2">
        SWITCH(mySwitchComponent).to(b, c)
    </chain>
</flow>